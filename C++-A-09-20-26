//C++ LANGUAGE

// c++ syntax

#include <iostream> // gives us access to std::cout, std::cin

int mean() {
	std:: << "Hello World!" << std::endl;
	return 0;
}


//Variable, types, and I/O
#include <iostream>
#include <string>
using namespace std;

int main() {
	//---- Declaring cariables of different types ----- 
	int age =20;		//whole numbers
	double price = 19.99; 	//decimal numbers (double precision)
	float wieght =65.5f; 	//decimal numbers (single precision note the 'f')
	char grade = 'A'; 	//a signle character (single quotes)
	bool isStudent = true; 	//true or false
	string name ="Maria"; 	//text(need <string> header)
	
	//---- Output using cout ----
	cout <<"Name: "<< name << endl;
	cout <<"Age: "<< age << endl;
	cout <<"Price: "<<price << endl;
	cout <<"Weight: "<< weight << endl;
 	cout <<"Grade: "<< grade << endl;
	cout <<"Is student? "<< isStudent << endl;  // prints 1 for true, 0 for false

	
	/---- Getting input using cin -----
	string userCity;
	cout <<"/nEnter your city: ";
	cin >> userCity; // reads one word (stops at whitespace)
	cout << "You live in: " << userCity << endl;

	//-----Reading a full line (including spaces)----
	cin.ignore(); // clears leftover newline character from previous cin
	string fullSentence;
	cout << "Enter a sentence about yourself: ";
	getline(cin, fullsentence); //reads the entire line 
	cout << "you said: " << fullsentence << endl;
	
	return 0;
}
//Operator and Expressions
#include <iostream>
using namespace std;

int main() {
 	int a = 10, b = 3;

  	//---- Arithmetic operators ------
	cout << "a + b = " << (a + b) << endl; //addition
	cout << "a - b = " << (a - b) << endl; // subtraction
	cout << "a * b = " << (a * b) << endl; // multiplication
 	cout << "a / b = " << (a / b) << endl; // integer
	division -> 3 (drops decimal!)
	cout << "a % b = " << (a % b) << endl; // modulus ->
remainder = 1

	double x = 10, y = 3;
	cout << "x / y = " << (x / y) << endl; //3.33333 ->
floating point division

	//----- Relational operators (return bool: 1 or 0) -----
	cout << "/na == b: " << (a == b) << endl;
	cout << "a != b: " << (a != b) << endl;
	cout << "a > b: " << (a > b) << endl;
	cout << "a < b: " << (a < b) << endl;
	
	//----Logical operators----
	bool p = true, q = false;
	cout << "np&& q: " << (p && q) << endl; // AND -> false
	cout << "p || q: " << (p || q) << endl; // OR -> true
	cout << "!p: " << (!p) << endl; // NOT -> false

	//---- Assignment operators ------
	int score = 50;
	score += 10; //same as score = score + 10
	cout << "/nscore after +=10: << score << endl;
	score *= 2; 	// same as score = score * 2
	cout << "score after after *= 2: " << score << endl;
	//------ Increment / decrement --------
	int counter = 5;
	cout << "ncounter++ (post): " << counter++ endl; //prints 5, THEN increments
	cout << "counter now: " << counter << endl; // now 6
	cout << "++counter (pre): "++counter << endl; // increments THEN prints -> 7
	
	return 0;

//control Flow(if/else,switch,loop)
#include <iostream>
using namespace std;

int main() {
 	//--- if / else if / else ---
	int score;
	cout << " Enter your exam score: ";
	cin >> score;

	if (score >= 90) {
	  cout << "Grade : A" << endl;
	} else if (score >= 80) {
	  cout << "Grade: B" << endl;
	}else if (score >=70) {
	  cout << "Grade: C" << endl;
	} else }
	   cout << "Grade: F << endl;
	}

	//---- swtch statement ---- 
	int day; 
	cout << "/nEnter a day number (1-7): ";
	cin >> day;

	switch (day) {
	case 1: cout << "Monday" << end;l break;
	case 1: cout << "Tuesday" << end;l break;	
	case 1: cout << "Webnesday" << end;l break;
	case 1: cout << "Thursday" << end;l break;
	case 1: cout << "Friday" << end;l break;
	case 1: cout << "Saturday" << end;l break;
	case 1: cout << "Sunday" << end;l break;
	default: cout << "Invalid day" << endl; breal;
	}

	//---- for loop ----
	
